# Field Atlas 1.1.9

This build fixes Android hardware-back handling from the Device benchmark
screen. Hardware back now returns to More and stops an active benchmark rather
than exiting Field Atlas; the visible top-left back control remains available.
